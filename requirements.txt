# TechNova World v4.0 — Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: Python 3.14+ compatibility — versions updated accordingly.
# Pillow 10.x does NOT build on Python 3.14 (setuptools __version__ bug);
# use Pillow>=11.0 which ships pre-built wheels for Python 3.14.

requests>=2.31.0        # API calls (Gemini, LinkedIn, RSS)
Pillow>=11.0.0          # Image watermarking (>=11.0 required for Python 3.14+)
reportlab>=4.2.0        # LinkedIn PDF carousel
python-dotenv>=1.0.0    # Local .env file support
schedule>=1.2.1         # Local interactive scheduler (run.py menu option 15)
flask>=3.1.0            # Web dashboard
gunicorn>=23.0.0        # Production WSGI server (for Render web service)

